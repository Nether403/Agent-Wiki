# Research (not product)

Notes, zips, and harvest lists that fed Phase 2. They are not part of the runtime registry, MCP, or CI.

The living product is:

- `packages/registry/src` — component source
- `catalog-stats.json` — generated counts
- `catalog-contract.json` — MCP tool contract
- `packages/audit-linter` — anti-slop rules
